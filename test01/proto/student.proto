syntax = "proto3";

package student;

service Hello {
    rpc Hello(Student) returns (Response);
}

message Student {
    string name = 1;
    int32 age = 2;
    repeated string courses = 3;
}

message Response {
    string reply = 1;
}